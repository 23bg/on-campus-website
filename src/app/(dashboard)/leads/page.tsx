"use client";

export default function LeadsPage() {
    return (
        <main className="mx-auto max-w-4xl p-8 text-center">
            <h1 className="text-2xl font-semibold">Leads dashboard will be re-enabled soon</h1>
            <p className="mt-4 text-muted-foreground">Current build configuration is using a temporary placeholder to satisfy routing and app integrity.</p>
        </main>
    );
}
